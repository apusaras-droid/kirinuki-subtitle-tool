# 切り抜き動画工房 仕様準拠チェックリスト

最終更新: 2026-07-18

`[x]` は実装または自動テストで確認済み、`[ ]` は追加の実機・配布確認が必要な項目です。依存ソフトを必要とする機能は、コード経路の存在と実環境での成功を分けて判定します。

## 1. プロジェクトと入力

- [x] ローカル動画を読み込める
- [x] `mp4`, `mkv`, `mov`, `webm` を入力候補として扱える
- [x] FFprobeで長さ、解像度、FPS、音声情報を取得できる
- [x] 複数音声トラックを列挙・選択できる
- [x] 指定区間を時刻入力またはプレビュー位置から設定できる
- [x] 選択音声を16kHz、モノラルWAVへ抽出できる
- [x] 動画名と日付を基にプロジェクトを作成できる
- [x] プロジェクト一覧から再開、リネーム、削除できる
- [x] 元動画を破壊的に編集しない
- [x] クライアント入力パスをプロジェクト境界内へ制限する

## 2. 字幕作成と発話解析

- [x] `whisper.cpp` を既定エンジンとして文字起こしできる
- [x] CPUとAMD Vulkan GPUを排他的に選択できる
- [x] GPU厳格モードでCPUへの黙示フォールバックを拒否できる
- [x] `openai-whisper` と `faster-whisper` の実行経路がある
- [x] ローカル字幕作成プリセットを選択し、詳細設定へ反映できる
- [x] Whisper本文とSilero VADの発話区間を別データとして扱える
- [x] `silencedetect`, `vad`, `hybrid` を選択できる
- [x] 声抽出音声をVADまたはWhisperの解析用に選択できる
- [x] 発話前後余白、最短発話、最短無音、結合間隔を設定できる
- [x] Whisper、VAD、自動、手動の開始・終了候補を保持できる
- [x] 開始と終了の採用元を別々に選択できる
- [x] 指定区間だけを再文字起こしして比較・反映できる
- [x] `transcript.json`, `original.srt`, `edited.srt` を保存できる
- [x] Gemini APIを任意の字幕作成・校正・提案経路として利用できる
- [x] Gemini APIキーをGit管理外の別設定に保存する
- [ ] openai-whisperの実機文字起こしを配布環境で確認する
- [ ] SpeechBrain話者分離の品質を対象素材ごとに実機確認する
- [ ] Demucs声抽出の依存導入と長尺安定性を配布環境で確認する

## 3. カットと時間軸

- [x] 無音区間の反対側から発話候補を作成できる
- [x] 発話前後の余白と短い間隔の結合を適用できる
- [x] 自動カット、手動カット、保護区間を編集できる
- [x] 保護区間をカットより優先し、非重複部分だけを削除できる
- [x] `keep_segments` を作成できる
- [x] 元動画時刻をカット後時刻へ区間単位で変換できる
- [x] 字幕を `output_start_sec` / `output_end_sec` へ再マッピングできる
- [x] カット後ジャンプ再生で簡易確認できる
- [x] `edit_plan.json` を保存・更新できる
- [x] プロジェクト内部の動画パスを相対保存できる

## 4. 字幕編集

- [x] 字幕一覧と動画プレビューを同時に表示できる
- [x] 本文、開始、終了、有効状態を編集できる
- [x] 現在の再生位置を開始または終了へ反映できる
- [x] 字幕の分割、結合、複数削除ができる
- [x] 字幕選択時のジャンプ再生とループ再生を切り替えられる
- [x] 字幕ごとのASSスタイルを編集できる
- [x] 編集結果を `edit_plan.json` と `edited.srt` へ保存できる

## 5. デコレーションとプレビュー

- [x] 通常ASS設定と装飾設定を別データとして扱える
- [x] テキスト、枠、文字連動、画面効果、拡大縮小を編集できる
- [x] 全体設定より字幕単位の設定を優先できる
- [x] 共通プリセットをプロジェクト外へ保存できる
- [x] 軽量プレビューを作成できる
- [x] カット後タイムラインで字幕と装飾をプレビューできる
- [ ] 全画面効果についてブラウザプレビューとFFmpeg出力の見た目一致を実機確認する

## 6. 最終出力

- [x] MP4またはMKVを出力できる
- [x] 通常ASSを外部ファイルとして出力できる
- [x] 装飾ASSを外部ファイルとして出力できる
- [x] 通常ASSまたは装飾ASSを動画へ焼き込める
- [x] ASS字幕トラックをMKVへ埋め込める
- [x] 互換用SRT出力経路を維持する
- [x] 出力先とファイル名を指定できる
- [x] 設定済み出力先へプロジェクト名で出力できる
- [x] 同名ファイルを上書きせず連番化できる
- [x] `edit_plan_final.json` を保存できる
- [x] MP4互換プロファイルで明示的なx264品質設定を使う
- [ ] すべての出力プロファイルを長尺実素材で確認する

## 7. 監査、安全性、配布

- [x] API、CLI、外部コマンドの監査ログが残る
- [x] JSONとSRTを原子的に保存し、Windowsロック時に再試行できる
- [x] 編集データの履歴を上限付きで保存できる
- [x] 自動クリーンアップは成功時だけ実行し、失敗時は中間物を保持する
- [x] GPLv3配布仕様と第三者ライセンス通知がある
- [x] FFmpeg取得時にGPLアセットとSHA-256検証を必須化する
- [x] セットアップ、依存検査、手動導入案内がある
- [x] 日本語フォント取得とOFL通知を用意している
- [x] リリース作成スクリプトがある
- [ ] クリーンな別PCでGPL配布物のセットアップを通し確認する

## 8. CLIと自動検証

- [x] `python -m backend` でCLIを起動できる
- [x] `probe`, `new-project`, `extract-audio`, `transcribe` を実行できる
- [x] `detect-silence`, `create-edit-plan`, `save-subtitles` を実行できる
- [x] `preview`, `export`, `cleanup` を実行できる
- [x] `run-pipeline --config` で一括実行できる
- [x] `report.json` と監査ログを出力できる
- [x] 外部ASS、焼き込み、埋め込みの出力モードをCLIで選べる
- [x] `scripts/verify-spec.ps1` で構成検査できる
- [x] Pythonテスト、Node状態管理テスト、フロントエンドDOM契約検査がある
- [ ] `scripts/smoke-test.ps1` を配布候補パッケージ上で実行する

## 直近の安定版検証

- Gitコミット: `5a6ab1e`
- Python: 66 tests passed
- Node状態管理: 1 test passed
- フロントエンド構文・DOM契約: passed
- 仕様検証: 22 checks passed（ローカルAPI一時起動下）
