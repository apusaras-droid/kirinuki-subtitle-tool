Source Code Offer

This binary release is accompanied by the corresponding source code for the same version.

This repository copy is a template only.

`scripts/package-release.ps1` replaces the application package copy with:

- the corresponding `source.zip` location;
- the source archive SHA-256;
- the exact release commit.

Do not distribute a binary application package containing this template text.
